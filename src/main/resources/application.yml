server:
  port: 9900
spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/customer
    username: afridi
    password: afridi
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

    show-sql: true
  main:
    web-application-server: servlet